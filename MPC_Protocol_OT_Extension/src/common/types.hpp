#pragma once

#include <cstdint>
#include <vector>

// Common types used throughout the project
typedef long long int ll;

// Constants
const int MOD = 1000007;

// Type definitions for shares
using ShareVector = std::vector<ll>;